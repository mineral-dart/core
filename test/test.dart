import 'package:mineral/core/api.dart';

void main() {
  /// var user = User();

  /// print(User(_id, _username, _globalName, _discriminator, _bot, _system,
  ///    _publicFlags, _flags, _decoration, _lang, premiumType));
}

//program addition
